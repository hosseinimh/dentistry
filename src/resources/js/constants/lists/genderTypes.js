import { GENDER_TYPES } from "../genderTypes";
import { genderTypes as strings } from "../strings/fa";

export const genderTypes = [
    { id: GENDER_TYPES.MALE, value: strings.male },
    { id: GENDER_TYPES.FEMALE, value: strings.female },
];
